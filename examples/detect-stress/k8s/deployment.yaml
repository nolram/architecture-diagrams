apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: platform
spec:
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: node:20-alpine
